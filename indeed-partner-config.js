// Public front-end identifiers supplied by Indeed after Publisher Plugin approval.
// Leave blank until Indeed enables the production/test domain and provides IDs.
window.INDEED_PARTNER_CONFIG=Object.freeze({
  partnerAppId:'',
  placementId:'',
  searchLimit:20,
  // Keep the official-plugin query intentionally simple. The direct-search UI
  // provides separate role presets instead of relying on undocumented Boolean
  // query semantics.
  searchWhat:'AI 在宅',
  searchWhere:'日本'
});
